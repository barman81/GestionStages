{% extends "base.html.twig" %}

{% block title %}Entreprises BlackList{% endblock %}

{% block body %}
    <!-- Page Content -->
    <div class="container">

        <div class="row">

            <!-- Blog Entries Column -->
            <div class="col-md-8">

                </br></br>
                <h1 class="my-4">BlackList Entreprises</h1>
                <table class="table table-striped table-middle-aligned">
                    <thead>
                    <tr>
                        <th scope="col">Nom Entreprises</th>
                        <th scope="col" class="text-center"><i class="fa fa-cogs" aria-hidden="true"></i> Actions</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for uneEntreprise in entreprises %}
                        <tr>
                            <td>{{ uneEntreprise.nomentreprise}}</td>
                            <td class="text-right">
                                <div class="item-actions">
                                    <a href="{{ path('noBlackListEntreprise', {'id':uneEntreprise.codeentreprise}) }}" class="btn btn-sm btn-success">
                                        <i class="fa fa-edit" aria-hidden="true"></i> Non BlackList
                                    </a>
                                </div>
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>

            </div>

            <div class="col-md-4">
                <div class="card my-4">
                    <h5 class="card-header"><a href="{{ path('showEntreprises') }}">Liste Entreprises</a></h5>
                </div>

            </div>

        </div>
        <!-- /.row -->

    </div>
    <!-- /.container -->
{% endblock %}
